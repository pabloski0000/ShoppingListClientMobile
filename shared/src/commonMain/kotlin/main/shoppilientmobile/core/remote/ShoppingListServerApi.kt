package main.shoppilientmobile.core.remote

open class ShoppingListServerApi {
    private val protocol = "https"
    private val host = "lista-de-la-compra-pabloski.herokuapp.com"
    protected val protocolAndHost = "$protocol://$host"
}